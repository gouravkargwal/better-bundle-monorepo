# FastAPI and Core Dependencies
fastapi>=0.109.0
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0

# Database
asyncpg==0.29.0

# Redis
redis==5.0.1

# Schema migrations
alembic==1.14.0

# SQLAlchemy async support
sqlalchemy[asyncio]==2.0.23
greenlet==3.2.4

# HTTP Client
httpx>=0.28.1

# JWT Authentication
PyJWT==2.10.1

# Kafka (replacing Redis streams)
aiokafka==0.11.0

# Environment and Configuration
python-dotenv==1.0.0

# Utilities
typing-extensions>=4.10.0

# Cross-encoder / bi-encoder for semantic product similarity (Phase 1)
sentence-transformers==3.4.1
pgvector==0.3.6

# Google Gemini / Vertex AI (Gorse LLM reranking + item embeddings)
google-genai==1.9.0

# OpenTelemetry (OpenObserve tracing)
opentelemetry-sdk==1.30.0
opentelemetry-api==1.30.0
opentelemetry-exporter-otlp-proto-grpc==1.30.0
opentelemetry-exporter-otlp-proto-http==1.30.0
opentelemetry-instrumentation-fastapi==0.51b0
opentelemetry-instrumentation-httpx==0.51b0
opentelemetry-instrumentation-sqlalchemy==0.51b0
opentelemetry-instrumentation-redis==0.51b0
# Process CPU, memory and thread count -- the saturation half of the four
# golden signals, which nothing else here reports. Pinned to the same
# 0.51b0 as the other instrumentations: they share the SDK contract and
# mixing versions is how you get a silently disabled instrumentation.
opentelemetry-instrumentation-system-metrics==0.51b0

# Development
pytest==7.4.3
pytest-asyncio==0.21.1
black==23.11.0
isort==5.12.0
flake8==6.1.0
