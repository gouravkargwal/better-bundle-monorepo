# ===========================================
# BetterBundle Production Environment
# ===========================================
# Copy this to .env.prod and update with your production values

# ===========================================
# APPLICATION SETTINGS
# ===========================================
NODE_ENV=production
DEBUG=false
LOG_LEVEL=info
# json in containers: stdout is scraped, not read by a human.
LOG_FORMAT=json
# Fraction of DEBUG/INFO log records shipped to OpenObserve. WARNING and above
# are NEVER sampled. Unset = 10% in production, 100% everywhere else.
# The decision is per-trace, so a sampled request keeps ALL of its log lines
# rather than a random tenth of them. Set to 1.0 during an incident to widen
# capture without a deploy; dashboards scale counts by 1/log_sample_rate, so
# volume panels stay correct either way.
# LOG_SAMPLE_RATE=0.1

# ===========================================
# DATABASE CONFIGURATION
# ===========================================
# Container-only: Postgres container reads DATABASE_URL directly.
DATABASE_URL=postgresql://REPLACE_ME:REPLACE_ME_WITH_SECURE_PASSWORD@postgres:5432/betterbundle

# ===========================================
# REDIS CONFIGURATION
# ===========================================
REDIS_URL=redis://redis:6379
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=REPLACE_ME_WITH_SECURE_PASSWORD
REDIS_DB=0

# ===========================================
# KAFKA CONFIGURATION
# ===========================================
KAFKA_BOOTSTRAP_SERVERS=kafka:9092

# ===========================================
# GOOGLE AI (Vertex AI) - Item embeddings
# ===========================================
AI_PROVIDER=vertex
VERTEX_PROJECT_ID=your_gcp_project_id
VERTEX_LOCATION=us-central1
GOOGLE_APPLICATION_CREDENTIALS=/etc/gcp/service-account.json
AI_EMBEDDING_MODEL=gemini-embedding-001
AI_EMBEDDING_DIMENSIONS=768
AI_CHAT_MODEL=gemini-2.0-flash-lite
# GEMINI_API_KEY removed: unused while AI_PROVIDER=vertex.

# ===========================================
# EXTENSION BUILD-TIME CONFIG (Shopify CLI injects these)
# ===========================================
# Set before `shopify app deploy`. Shopify CLI replaces process.env.* in
# extension bundles at build time.
VITE_BACKEND_URL=https://api.betterbundle.online
VITE_SHOPIFY_APP_URL=https://betterbundle.online

# ===========================================
# SHOPIFY APP CONFIGURATION (Production)
# ===========================================
SHOPIFY_API_KEY=your_prod_shopify_api_key
SHOPIFY_API_SECRET=your_prod_shopify_api_secret
SCOPES=read_products,write_products,read_orders,write_orders,read_customers,write_customers
SHOPIFY_APP_URL=https://your-production-domain.com
# SHOPIFY_APP_SESSION_SECRET removed: not read by application or extension code.
# SHOPIFY_APP_HANDLE removed: not read by application code.

# ===========================================
# OBSERVABILITY (OpenObserve)
# ===========================================
OPENOBSERVE_ENDPOINT=http://openobserve:5080
OPENOBSERVE_ORG=default
OPENOBSERVE_API_KEY=
OPENOBSERVE_ADMIN_PASSWORD=REPLACE_ME_WITH_SECURE_PASSWORD
# ZO_* vars removed: OpenObserve reads them from docker-compose inline env blocks.

# ===========================================
# PYTHON WORKER
# ===========================================
PYTHON_WORKER_API_URL=http://python-worker:8000

# ===========================================
# SECURITY & ENCRYPTION (Production)
# ===========================================
SECRET_KEY=REPLACE_ME_WITH_SECURE_PASSWORD
ADMIN_CRON_SECRET=REPLACE_ME_WITH_SECURE_PASSWORD
ADMIN_USERNAME=admin
ADMIN_PASSWORD=REPLACE_ME_WITH_SECURE_PASSWORD
ADMIN_SECRET_KEY=REPLACE_ME_WITH_SECURE_PASSWORD
# JWT_SECRET and ENCRYPTION_KEY removed: defined here but never read by application code.